# 实验日志 · perf/lightning-sprint

> 每改动一组跑护栏(typecheck + vitest 1315 + cargo test 78 + bundle 棘轮),一次 commit

## P0 棘轮修复

| # | 改动 | 结果 |
|---|---|---|
| 0.1 | `_lib.mjs` logicalName 正则 `[0-9a-f]` → `[0-9a-zA-Z]`(rolldown base64 hash 含大写);assert 把 `untracked` 也判 FAIL;capture 前检测 dist 跨构建残留 | 旧 ledger 全部 REMOVED,重建 474→136 key(清理 338 个历史残留),RATCHET GREEN |
| 0.2 | 新增 `assert-entry-budget.mjs`:登记 dist/index.html 实际 eager(modulepreload+script+css)资源的 gzip 总量与集合,只许降;新条目必须登记 | 基线登记 10 文件/622.51KB gz |

## P1 冷启动

| # | 假设 | 改动 | 结果 |
|---|---|---|---|
| 1.1 | 恢复文件的 700ms 死延迟是白给的慢 | AppLayout.tsx: 删 setTimeout(700),requestIdleCallback timeout 1500→200 | 待配对测 |
| 1.2 | settingsService 一行 import 拖 docx-vendor 353KB 进首屏 | import 从 `./word`(barrel)改 `./word/config` + `./word/types`(类型) | **eager 622.51→510.67KB gz(-112KB)**,docx-vendor/word chunk 移出 modulepreload |
| 1.3 | TerminalPanel 无条件挂载致 353KB chunk 启动即载 | `terminalMounted` 状态:首次 visible 才渲染,之后保持挂载(不反复起 PTY) | TerminalPanel 移出首屏(index.html 0 引用) |
| 1.4 | editor-language-vendor(107KB) 可挪出首屏 | **放弃**:它是 CM6 编辑器真实依赖(createMarkdownExtensions 静态引 lang-markdown),编辑器启动就要;挪走只延迟 100ms 非真收益 | 不做 |
| 1.5 | SettingsPage 预热抢启动带宽 | 预热改 requestIdleCallback(timeout 3000)/fallback setTimeout 1500 | 待配对测 |
| 1.6 | 启动前 3 次串行 reg.exe(约 150ms) | lib.rs: `registry_value_exists` 改 RegOpenKeyExW/RegQueryValueExW 直读(微秒级),reg.exe 保留为回退 | 待配对测 |
| 1.7 | 文件内容 IPC 走 JSON number[] 3-4 倍膨胀 | **本轮未做**:需改 tauri::ipc::Response + 前端 invoke 类型,牵面大,收益主要在大文件;记录到下轮 | 不做 |
| 1.8 | Cargo 无 release profile | `[profile.release] lto="thin" codegen-units=1 strip=true` | 待测(exe 预计 23.9MB→~15MB) |
| 1.9 | claude.ai 式静态骨架 | 按约定:若 1.1-1.8 后达标(<1200ms)则不引入(避免草稿覆盖类风险) | 待定 |

## P2 打字流畅度

| # | 假设 | 改动 | 结果 |
|---|---|---|---|
| 2.1 | 每键 ≥3 次全文 doc.toString() | markdownAnalysisService 新增 `analyzeMarkdownState`(WeakMap<EditorState> 缓存)+ `materializeDocSource`;headingFold/previewSync/mathPreview 三处全切换 | 同一按键只物化 1 次字符串+1 次分析;289 编辑器测试绿 |
| 2.2 | 缓存 hash 是 O(n) 逐字符 ×2 | 同上:state 缓存命中用 docLength+state 身份,不再跑 hashSource | 并入 2.1 |
| 2.3 | CJK 统计正则极贵(实测 1.3ms/次) | calculateStats 改码点范围单遍遍历(0.3ms/次,4x);范围集与 \p{Script=..} 在常用 CJK 区块验证等价 | 14 分析测试绿 |
| 2.4 | 微信预览面板每键全文 unified(仅 useDeferredValue) | `useDebouncedValue(useDeferredValue(source), 260)` 与 Word 预览同口径 | 7 测试绿 |
| 2.5 | Word 预览整篇重渲 | **不做**:已有 260ms debounce+deferred,增量分页重写复杂度高、收益场景窄(面板打开时),符合"几百行换几毫秒不做" | 不做 |

## P3 React 渲染

| # | 假设 | 改动 | 结果 |
|---|---|---|---|
| 3.1 | TreeNode 无 memo,1000+ 文件每键 1000+ render | TreeNodeInner + memo();dirtyPaths memo 依赖改序列化 key(打字期 Set 不重建) | 2 测试绿 |
| 3.2 | agentChangedPaths 内联 new Set 每 render 新引用 | AppLayout useMemo 包裹 | TS 绿 |
| 3.3 | AppLayout 整树每键重渲染(结构性) | **本轮不做**:3128 行大手术;待 1/2/3.1/3.2 收益测量后再评估是否需要 | 待定 |

## P4 Rust

| # | 假设 | 改动 | 结果 |
|---|---|---|---|
| 4.1 | 4 个同步命令卡主线程(agent_detect 最长 5s) | read/stat/write_opened_document、agent_detect、scan_artifacts、list_directory_entries 全部 async + spawn_blocking | 78 Rust 测试绿 |
| 4.2 | 文档 watcher 无防抖,atomic_write 连发事件风暴 | 120ms 合并窗口(Arc<Mutex> 登记+线程延迟 emit) | 编译绿 |
| 4.3 | 保存后整文件读回算指纹 | write_opened_document 直接用刚写入的 bytes 算 hash+stat,不再 fs::read 一遍 | 测试绿 |
| 4.4 | agent-stdout 逐行 emit,stream-json 时 IPC 风暴 | Rust 16ms/64 行攒批 emit `lines[]`;前端 onAgentStdout 拆回逐行,两个消费者零改动 | TS+cargo 绿 |

## 护栏状态(每批后)

- typecheck: 绿
- vitest: 1315/1315 绿(中途一次 3 失败系与后台 cargo build 抢资源,复跑两次均全绿)
- cargo test: 78/78 绿
- bundle 棘轮: GREEN(需在最终构建后 recapture)
- entry 棘轮: 510.68KB gz(1.2/1.3 后)
