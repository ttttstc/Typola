# Typola Architecture

Typola is a Tauri v2 desktop Markdown editor built with React 19, TypeScript, Vite 8, CodeMirror 6, a remark/rehype export renderer, and a Rust backend.

## Core Shape

- `src/app/AppLayout.tsx` owns the document lifecycle, lightweight opened-file tab state, editor mode, preview panels, update flow, and terminal panel visibility.
- `src/components/FileTreePanel.tsx` provides the left workspace file tree. It opens a user-selected directory, lazily expands folders, and delegates file reads back to the same controlled open path used by dialogs and OS file association. The workspace file tree is collapsed by default.
- CM6 表格的多选、行列手柄、导航、剪贴板、撤销和行列事务均由 `codemirror-markdown-tables` 持有；`TableContextMenu` 统一呈现 Obsidian 风格中文表格右键菜单，`tableInteractionExtension` 将菜单动作转发给上游行/列手柄，不维护表格选区、序列化或行列事务模型。整表删除通过 CM6 GFM 语法树定位 `Table` 源码节点并提交单笔 transaction；表格尾部退格先选中整表、再次退格才删除。表格内右键通过当前单元格反查上游行/列手柄，因此无需先手动框选整行或整列。非表格区域的 TSV/CSV/HTML 粘贴仍走兼容适配，表格 widget 内的粘贴和快捷键完全交给上游。
- `src/components/editor/cm6/Cm6MarkdownEditorPane.tsx` is the only Markdown writing entry. `src/components/EditorPane.tsx` provides its CM6 source and transaction bridge, while `src/components/editor/cm6/createMarkdownExtensions.ts` centralizes extension construction. Writing mode enables atomic-editor task/image/inline-preview widgets plus the open-source `codemirror-markdown-tables` table widget, and Typola-owned KaTeX and Mermaid widgets; source mode keeps live preview disabled. The table widget owns cell/range selection, row/column controls, clipboard operations, navigation, and cell-level CM6 editors while synchronizing edits back to the raw Markdown source. `EditorPanePaperBackground` wraps the CM6 editor with an optional static `@paper-design/shaders-react` paper texture; it supplies an opaque theme-colored base image because the package's empty image is transparent and otherwise produces a transparent framebuffer. `settingsService.editorPaperBackground` persists the toggle, maps `--theme-canvas` / `--theme-paper` to shader colors, and deliberately disables the effect for `night-current` and `abstract`. Math and Mermaid blocks use a shared source-hash/theme cache, render only when CM6 materializes the widget in the viewport, preserve source while the cursor is inside a block, and display a non-blocking error card on render failure. Preview synchronization and Mermaid IDs are scoped to each editor extension instance. `previewSyncExtension` supplies active heading indexes to the floating TOC, and `reviewMarkExtension` resolves review anchors and decorates their CM6 source lines without mutating editor DOM. `src/types/editorCore.ts` defines the stable `TypolaEditorKernel` contract for Markdown source reads/writes, selection, range replacement, current-document search, anchor validation, and search reveal commands.
- CM6 performance-sensitive extensions are isolated in `Compartment`s created by `createLivePreviewExtensions`: mode/theme preview widgets, zoom, preview sync, review marks, task/link interactions, image assets, and heading folding have independent lifecycles. Prop changes reconfigure only affected compartments; `EditorPane` keeps the `EditorView` in a ref and binds selection/scroll listeners when the view is created, so editor creation does not trigger a React tree rerender. Preview scroll reads cached headings and throttles only viewport changes; document changes update synchronously. Heading fold decorations reuse a doc-change-only `StateField`, inline math rebuilds only when selection crosses a known math range, and Markdown analysis cache hits refresh true LRU recency.
- `src/components/FindReplacePanel.tsx`, `QuickOpenPanel.tsx`, and `EditAssistPanel.tsx` provide on-demand editing utilities. Their matching, recent-file, statistics, and snippet logic lives in small service modules so the main editor path stays light.
- `src/components/WordPaperPreviewPane.tsx`, `src/services/word/*`, and `src/services/wordExportService.ts` provide Word-style preview and background `.docx` export. Export paths are derived automatically from the current document path, with unsaved documents falling back to the user downloads directory.
- `src/components/WechatPreviewPane.tsx` is the HTML preview compatibility component. User-facing copy is generic rich HTML export/copy.
- `src/components/HtmlPreviewPane.tsx` and `src/services/htmlPreviewService.ts` provide a read-only HTML preview pane for AI-generated HTML artifacts. The right-side panel reads the file via `plugin-fs`, inlines local CSS/JS/images/media referenced from the artifact directory into a sandboxed iframe `srcDoc`, and exposes refresh / open-in-system-browser / return-to-artifact-center / close actions. `htmlPresentationService` reuses the same generic HTML build/inline helpers from `htmlPreviewService`, keeping the presentation bridge script separate.
- `src/services/pdfExport.ts` renders the current Markdown into a PDF-specific HTML fragment, opens the native save dialog so users choose the target file/folder, and sends the selected path to Rust. The Rust `export_pdf` command creates a hidden offscreen WebView2 window, injects that HTML into `public/pdf-print-shell.html`, waits briefly for fonts/images on a fail-open path, and then calls `Page.printToPDF` on Windows. Export progress and results are reported through a non-blocking top-right toast.
- `src/services/markdownExportRenderer.ts` is the shared remark/rehype Markdown-to-export-HTML pipeline. HTML preview, Word paper preview, WeChat preview, and PDF export call it with Markdown source, then run their format-specific wrapping/export steps. It provides GFM, code highlighting, KaTeX, Mermaid SVG post-rendering, local image resolution, and AST-level HTML sanitization without Vditor. `.docx` export keeps its existing parser/Pandoc fallback. PDF export uses the same native save dialog as Word, so users choose its target file/folder before the Rust WebView print command runs.
- HTML file export resolves local image `src` values against the source Markdown file, copies readable assets to the saved HTML file's configured sibling directory, and rewrites only copied references. Remote/data URLs and failed copies retain their original source, so export remains fail-open.
- CM6 live preview additionally owns source-preserving YAML frontmatter, footnote, and raw HTML decorations. The raw HTML widget is sanitized before DOM insertion and falls back to Markdown source while its range is selected; exported HTML continues through the shared AST sanitizer.
- `src/components/TerminalPanel.tsx` uses xterm.js for the bottom terminal panel.
- `src/services/themeRegistry.ts` defines the Typola theme registry. The app ships five complete appearance themes: `plain-paper`, `night-current`, `ink-basin`, `abstract`, and `brutalist` (shown as 素笺 / 深海 / 墨韵 / 抽象 / 粗野). Settings persist `themeId` plus `themeOptions`; unknown ids fall back to `plain-paper`, while legacy `theme: "dark"` settings migrate to `night-current`. `brutalist` is a Neo-brutalism × vintage tool-manual theme — 0 border-radius, 1px pure-black borders, 5×5px hard shadow on interactive surfaces with hover/active translate feedback, a 30px grid paper background, and Source Han Serif SC / SF Mono font token overrides. The shared UI font is the self-hosted Source Han Serif SC variable-font subset under `public/fonts/`, with system fallbacks documented beside the asset.
- `src/components/conversation/ConversationPanel.tsx` provides the left AI Workbench conversation surface for Skill OS M1.
- `src/hooks/useAgentSession.ts` and `src/services/agent/*` bridge AI Provider headless stdout into typed message state and UI-friendly diagnostics. Claude Code and OpenCode are sendable providers; Codex CLI is registered for detection only until a parser/execution path exists.
- `src/services/documentWatchService.ts` bridges file watcher commands/events for the active document.
- `src-tauri/src/lib.rs` owns system file open/read/write commands, directory listing, document watching, single-instance forwarding, terminal PTY commands, AI CLI detection (`agent_detect`), and the headless AI Provider session commands used by the AI Workbench.

## Desktop File Flow

- File open paths can come from the native open dialog, drag/drop, OS file association, second-instance forwarding, or reopen-last-file.
- `read_opened_document` and `write_opened_document` are the controlled Rust read/write path for supported document types. `saveFileAs` uses the same Rust write command in Tauri builds, so unsupported extensions are rejected consistently.
- `rename_opened_document` renames an already-saved writable document within its current directory. The frontend exposes it from the title/tab rename dialog and syncs the resulting path back into tabs, recent files, and the active document state.
- Tauri capabilities keep filesystem plugin access scoped to common user document locations and dialog-granted paths. The desktop CSP allows Vditor renderer requirements, but does not allow `script-src 'unsafe-inline'`.
- Opening another document creates or switches to a lightweight file tab instead of replacing the active document. Dirty state is stored per opened tab; dirty tabs and matching file-tree entries show a leading `*`. The active file state is mirrored into refs for close confirmation, so a just-edited tab cannot close before the tab snapshot catches up. Closing a dirty tab uses the same save / discard / cancel decision model as window close; choosing save writes the tab first and aborts the close if saving fails or Save As is cancelled. The Tauri close-request listener is registered once and reads that live dirty ref. Close requests are always intercepted first; dirty sessions ask whether to save before closing, discard changes, or cancel. If the user chooses save, Typola saves every dirty writable tab before closing and aborts if any save fails. After the decision Typola explicitly calls `window.destroy()`, allows repeated close events while destruction is in progress, and falls back to the Rust `force_close_main_window` command if the frontend close API fails. The tab bar is hidden while only one file is open.
- `list_directory_entries` lists a user-selected workspace directory for the file tree. It includes directories and supported document files, excludes hidden/build-heavy folders such as `.git`, `node_modules`, `dist`, and `target`, and sorts folders before files.
- The left workspace tree and right preview panel are width-adjustable. The right Word preview can shrink to a compact width and keeps paper pages close to the resizer to reduce dead space.
- `tauri-plugin-single-instance` forwards secondary process argv paths to the running window through the existing `opened-paths` event.
- The active file is watched by Rust `notify` through `watch_opened_document` / `unwatch_opened_document`. External changes emit `file-changed`; the frontend suppresses events that arrive within 1.5s of a known self-write.
- If reopen-last-file fails, the stale path is cleared so the next launch does not retry a permanently missing document.
- Recently opened files are stored as lightweight local metadata in `localStorage` and filtered in memory for `Ctrl+Shift+P`; Typola does not scan the filesystem or workspace during quick open.
- Pasted clipboard images are written through the Rust `write_attachment_file` command into a sibling `assets/` directory for the current document, returning a relative Markdown image path to the editor.

## Editing Utilities

- File search uses `documentSearchService` with debounced panel input. It supports plain text, regex, case-sensitive, and whole-word matching, with a hard match cap to keep very large documents responsive.
- Document statistics use `documentStatsService` on a debounced copy of the current document, so typing does not synchronously recompute counts.
- Both writing and source modes expose precise CM6 transaction-backed insert, replace, undo, and reveal operations through `TypolaEditorKernel`. Find/replace supplies source ranges to `replaceRanges`, so single and all replacements each become one CM6 history entry instead of a React-side content rewrite.
- Toolbar controls, Markdown shortcuts, and the editor context menu all call `TypolaEditorKernel.format`; the sole CM6 formatter dispatches each document change, including quote depth, link URL, format clearing, and fenced-code language edits.
- The CM6 formatter also provides paired inline toggles for underline, superscript, subscript, and source-preserved `==highlight==`; the export pipeline converts the latter to sanitized `<mark>` markup.
- Local image resolution only expands asset scope for absolute paths inside the current document directory; metadata serialization escapes user text before creating HTML image markup. CM6 editing dialogs are dismissible by Escape, outside click, and trapped Tab navigation.
- Mermaid rendering is centralized in `mermaidRenderer`. It lazy-loads the Mermaid library, converts `language-mermaid` code blocks to SVG for preview and export renderers, and leaves syntax failures as source plus an inline error message. The same renderer feeds Markdown preview, AI/review previews, HTML preview output, and Word preview artifacts.

## Terminal

The terminal is implemented with Tauri commands plus event streaming:

- `terminal_create` resolves a working directory and shell, creates a `portable-pty` PTY, and emits terminal output through `terminal_data`.
- `terminal_write`, `terminal_resize`, `terminal_kill`, and `terminal_clear` are exposed as front-end commands.
- `terminal_data` carries raw bytes from Rust. The frontend decodes bytes with `TextDecoder` using the current default encoding, which avoids Rust-side lossy UTF-8 replacement on Windows shells.
- Terminal registry locks are held only while resolving session handles. PTY write/resize/kill operations run outside the global registry lock so one blocked session does not freeze other sessions.
- `terminal_clear` writes `ESC[3J ESC[2J ESC[H` to the PTY in addition to clearing the xterm viewport.
- Windows shell resolution prefers `pwsh.exe`, then `powershell.exe`, then `cmd.exe`. Current release documentation and distribution target Windows.
- The front end derives terminal cwd from the selected workspace tree first, then the opened file path; otherwise Rust falls back to the user home directory.

## Theme System

- Typola has two persisted appearance systems selected by `appearanceColorSystem`: `static-theme` and `define-color`. `AppLayout` is the only runtime owner and calls `applyAppearanceToDocument`; the dispatcher applies either the existing registry theme or the Define system and sets `html[data-color-system]`. New installations default to `define-color` with a white unselected base; Settings exposes explicit custom/theme mode controls, while choosing any theme card switches to `static-theme` without deleting the last Define configuration.
- `AppLayout` reapplies the persisted Define color settings on startup and whenever the settings snapshot changes; this prevents the color picker state from being stored but visually lost after relaunch.
- Static themes keep `themeId`, `themeOptions`, `themeRegistry.ts`, and generated `themes.css`. Define state lives in `defineColorSettings`, `src/services/defineColorSystem/*`, `src/components/defineColor/*`, and `define-color-system.css`. Define continues to own the `--dc-*` palette; a scoped `html[data-color-system='define-color']` semantic bridge maps application-wide `--theme-*` consumers onto that palette so existing controls follow the custom color without importing static theme values into Define derivation.
- The Define system ports the reference application's OKLCH palette derivation: one base color produces heavy/light bases, hue-adjusted neutral scales, text/icon/border/background aliases, optional ±25° gradient stops, and pattern composition. During hue drag, the wheel handle and mask receive the current pointer coordinate immediately while a separate visual hue ref prevents throttled React state from rolling the handle backward; app-wide token application remains rate-limited to prevent global style recalculation on every pointer event. Pointer release and clicks persist normalized settings.
- The Define popover is mounted through Floating UI Portal and contains the fixed-radius Hue Wheel, three-state saturation cycle (soft/balanced/vivid, balanced by default), 50 ordered presets, Solid/Gradient toggle, nine-pattern cycle, pattern-opacity slider, and bounded Surprise me randomizer. Persisted settings rehydrate the editor after loading, so reopening retains the exact selected color. Its two editor textures are byte-identical local assets and the eight Pattern files are official `demo.define.app` PNGs, so runtime use remains offline.
- The floating outline is an edge-triggered drawer: it stays visually hidden at the left edge until pointer or keyboard activation, and the toolbar can request it to open without pinning or covering the file tree. The hue wheel keeps its primary and gradient auxiliary handles on one immediate visual coordinate, while settings and Skill Hub surfaces use Define-specific contrast overrides rather than generic control backgrounds.
- `src/services/windowChrome.ts` converts the selected OKLCH base to sRGB and coalesces native chrome updates. On Windows, the Tauri `set_title_bar_color` command calls DWM caption/text color attributes so the native title bar follows custom selection with readable light/dark caption text; browser and non-Windows runtimes safely no-op.
- Theme colors are intentionally restrained: `plain-paper` preserves the classic light warm-white / warm-orange system, `night-current` uses a deep neutral stack with one cool accent, `ink-basin` is monochrome black/white/gray, and `abstract` is a Mondrian De Stijl palette (white canvas, pure black grid borders, Mondrian red / blue / yellow as accent / inserted / warning). `ink-basin` enforces that all canvas / surface / border / selection values are pure gray (`R = G = B`) so no chroma leaks into the monochrome stack. `plain-paper`'s warm tokens (selection, danger, ai-deleted, ai-primary, review-mark, warning) are tuned toward the Claude warm-light palette — selection is a near-neutral warm gray (`#dad6c9`), danger/ai-deleted are Claude-style sienna (`#bd6240`), and warning/ai-primary lean warm-amber / warm-taupe — so no token reads as pink against the cream background. `night-current` keeps a cool blue identity while pushing its warm tokens clearly into the orange/amber half of OKLCH (`#b17a5a` for danger/ai-deleted, `#c8985e` for warning) so they harmonize with the cool palette instead of clashing as pink-rose. `abstract` keeps its primaries pure and remaps terminal green/cyan/magenta aliases back to red or blue so the theme stays in red / blue / yellow / black / white. Shared panel/control/status colors are derived from the active theme tokens to avoid unrelated card and panel colors on the same page. SkillHub scene cards ignore per-scene category colors in ordinary themes and use neutral paper/border layers; only `abstract` restores per-scene accents, constrained to red, blue, yellow, black, and white. App-wide pink-prone OKLCH hardcodes (`.skill-hub-error`, `.selection-result-card-error`, `.diff-hunk-delete`, `.review-sidebar-item-tool-danger:hover`, `.file-tree-icon-doc`) have been moved to `--theme-danger` / non-pink OKLCH hues so no error / hover / focus state reintroduces pink.
- `npm run build:themes` generates `src/styles/themes.css` from the TypeScript registry. The generated file defines the `--theme-*` variable namespace for shell surfaces, paper, text, borders, accents, AI colors, review marks, editor colors, and terminal colors; `src/styles/app.css` imports it instead of carrying hand-written theme token blocks.
- `npm run audit:theme-contrast` checks the five theme registries against the WCAG relative-luminance thresholds used by editor text, secondary text, accents, status colors, paper, and canvas. The screenshot coverage in `e2e/editor-paper-bg.spec.ts` exercises all five themes and both toggle states while keeping three representative visual baselines.
- CodeMirror uses CSS variables in `createMarkdownExtensions`, so theme changes do not require rebuilding the editor. Vditor preview mode, Mermaid, AI floating surfaces, review marks, and xterm derive their host-specific theme values from the same theme registry. Existing xterm tabs refresh their options when `themeId` changes.
- `applyThemeToDocument` mounts `html[data-theme-transition='true']` during real theme switches and removes it after 260ms, letting app surfaces crossfade without changing the persisted theme contract.
- `themeOptions.reviewEnhanceMarks` controls stronger review markings when `html[data-doc-mode='review']` is active.
- Word / HTML / PDF export output does not follow the application theme. Export appearance remains controlled by export presets and export-specific renderers.

## AI Workbench

- The AI Workbench is provider-shaped. An AI Provider is a CLI backend such as Claude Code or OpenCode; each provider owns its executable path, optional model string, argument builder, stdout parser, diagnostics, and resume/session behavior.
- The right-rail `ConversationPanel` and `ConversationComposer` derive their backgrounds from `--theme-paper`, not from a hardcoded light tint, so the AI Workbench surface follows the active theme (warm cream under `plain-paper`, deep blue-black under `night-current`, pure white under `ink-basin`).
- `agent_detect` runs the selected provider's version command through Rust and supports either PATH lookup or a user-configured executable path. On Windows, bare commands should resolve to `.cmd` / `.exe` wrappers before `.ps1`, so npm-installed CLIs work without PowerShell execution-policy failures. When a npm `.cmd` wrapper points at a real executable such as `opencode.exe`, Rust starts that executable directly; only unresolved wrappers fall back to `cmd.exe` with forced argument quoting. Child processes spawn with `CREATE_NO_WINDOW` so wrappers do not flash a console.
- The Settings → AI CLI section surfaces runtime-specific availability and version checks. Claude Code keeps an optional Claude model string; OpenCode adds the same optional path and model shape plus install/docs guidance. Codex CLI is registered as a detection-only runtime: Typola can detect `codex --version`, but Codex is intentionally excluded from the Composer sendable provider list until a parser/execution path exists. Empty path values use the runtime default command (`claude`, `opencode`, or `codex`), and empty model values let executable CLIs choose their default model. OpenCode run and detect failures are mapped into user-facing diagnostics for missing CLI installs, bad executable paths, invalid `provider/model` strings, and auth failures.
- The active provider switcher lives in the AI Workbench composer footer as a compact pill button group, not as the primary control in Settings. Changing the active provider confirms the switch and starts a new provider-bound conversation.
- Skill OS M1 uses separate headless commands (`agent_session_start`, `agent_session_resume`, and `agent_session_cancel`) instead of the PTY terminal. Rust spawns the selected provider with ordinary pipes, writes prompts through stdin or the provider's matching non-interactive input path, and forwards each stdout line as the `agent-stdout` Tauri event.
- Claude Code currently runs with `claude -p --input-format text --output-format stream-json --verbose`, plus session, model, plugin directory, and allowed-directory arguments. OpenCode follows the same integration style with `opencode run --format json`, an optional model argument, the selected working directory, and permission flags only where they match OpenCode's documented CLI behavior. OpenCode session ids are CLI-owned `ses_...` values, so Typola does not pass its internal UUID as `--session`; later turns use `--continue` from the conversation artifact directory.
- Conversation sessions are provider-bound. Switching editor files does not reset message history, but switching between Claude Code and OpenCode cancels any active run, silences late stdout for that run, waits for the exit event or timeout, and then starts a new AI conversation instead of resuming the previous provider's CLI session.
- Frontend parsing is isolated in provider parser modules under `src/services/agent/`. The Claude Code parser maps stream-json into assistant text, thinking deltas, tool cards, usage summaries, and diagnostics. The OpenCode parser maps verified JSON events into the same UI event vocabulary: `text` parts become assistant text, `tool_use` parts become tool cards and tool results, `step_finish` becomes completion/usage state, and reasoning/thinking text is surfaced when the OpenCode stream includes it.
- The AI Workbench effective workspace root is resolved in this order: the user-selected `aiWorkspaceRoot`, then the user's default home directory. It does not fall back to the file-tree root or the current document directory. This root is used for provider cwd, allowed/read directories, `.typola-output`, artifact generation/scanning, and the Composer working-directory display. Choosing a workspace in the Composer WorkingDirPicker persists `aiWorkspaceRoot` and takes precedence over the default directory.
- Changing the AI workspace starts a new conversation: the frontend confirms the change, cancels any active headless run, clears local messages, and lets the next prompt create/resume a fresh in-memory Claude session for the new cwd.
- Composer context chips are prompt context for Claude Code: the current document and attached files are appended to stdin text as reference paths when they need to be introduced. The current document chip is removable for the current file, and switching files restores a new current-document chip without resetting the global conversation. The active conversation records the current-document path that was last injected, so Claude follow-up sends avoid repeating the same document in prompt text but inject a newly opened editor file.
- Visible context chips also produce provider reference paths on every send. Claude receives these paths through prompt text and allowed-directory arguments. OpenCode freeform chat keeps the prompt text unchanged and receives repeated `--file <path>` arguments each turn while the user's actual message stays the `opencode run` message argument. OpenCode SkillHub command conversations additionally append the same `参考以下文件` prompt block as Claude so command prompts can see the reference-file intent while still receiving `--file` paths. On Windows, npm `.cmd` wrappers are resolved to the real target executable where possible so shell metacharacters in the prompt are not reparsed by `cmd.exe`. OpenCode runs with its process cwd in the conversation artifact directory, but its `--dir` project directory points at the effective AI workspace root so file discovery matches the selected/default workbench directory instead of the empty `.typola-output` conversation folder.
- The Composer `+` menu exposes three Claude-native integrations for M1: file attachment, `.mcp.json` editing under the selected cwd, and Plugin directories. Plugin directories are persisted as `aiPluginDirs` and forwarded to Rust so `build_claude_headless_args` appends repeated `--plugin-dir <path>` pairs.
- SkillHub system templates and user-added local skills are scoped by AI Provider before rendering install actions. Claude scans `.claude/skills` and keeps the slash-command prompt flow; OpenCode scans global and project `.opencode/commands` / `.opencode/command` Markdown commands plus `opencode.jsonc` `command` entries, stores those additions as OpenCode-capable, shows a natural-language Composer prefill, and invokes them through `opencode run --command <name>` instead of injecting a Claude-style `/skill` prompt. In OpenCode command conversations, the text the user leaves in Composer is passed as the positional `<prompt>` after `--command <name>`; command templates may consume it through their own argument placeholders, while Typola still passes visible context files separately through `--file`. Built-in scene templates can declare OpenCode support; when a scanned local command matches a built-in template name, the scene card is immediately selectable without a separate custom scene addition. SkillHub install prompts and reload/error labels are provider-aware so OpenCode command setup is not described as Claude skill installation.
- For M2 artifact return, headless requests can pass provider-specific allowed-directory arguments where supported. Typola runs providers from `<effective-workspace-root>/.typola-output/<conversation>/` and appends a lightweight write guard to the CLI prompt, so newly generated artifacts are directed to the current conversation folder instead of the workspace root or source document directory. Provider-specific read/write access to the effective workspace root should be added only through documented CLI flags.
- Provider parsers may emit an `artifact_file` event when JSON output reports Write/Edit-style tool calls with a file path. The workspace watcher listens only to the effective `.typola-output/` directory as a provider-neutral fallback, avoiding recursive watches on the whole home/workspace tree. The artifact center scans `.typola-output/` through the Rust `scan_artifacts` command, which only accepts a canonical `.typola-output` root and returns manifest metadata plus legacy files without relying on frontend `fs:scope` grants for arbitrary workspaces. The UI shows artifacts as right-bottom filename chips and right-panel list entries; clicking a chip opens the file in the central editor, and `archive_artifact_to_workspace` moves a temporary artifact into the workspace with automatic name de-duplication. The artifact center persists lightweight `artifact.json` manifests and can overwrite or undo-overwrite a source document; those commands are guarded in Rust by canonical path checks, so the target must match the manifest/current document expectation or remain inside the explicit workspace root.
- The headless workbench coexists with the terminal-based flow-mode agent path. The left rail is a single state machine (`none` / `workspace` / `aiWorkbench`), so file tree and AI Workbench are mutually exclusive and never create a fourth column. The existing bottom PTY terminal remains unchanged and flow mode no longer auto-opens it.

## Product Rules

- Product name is `Typola`.
- Auto-save exists as a setting but defaults to off.
- Custom Word and HTML export presets are open by default with an 8-slot cap.
- Legal-industry-only UX, beta authorization UI, and complex-table original-view workflows are not part of the rewritten product.

## Packaging

- `npm run tauri dev` 运行时，Vite 会忽略 `src-tauri/target/**`，避免前端 dev server 监听 Rust 构建产物并在 Windows 上撞到 `app_lib.dll` 文件锁。
- Installer builds continue to use `tauri build` and produce Windows NSIS setup packages from `src-tauri/target/release/bundle/nsis/` plus MSI packages from `src-tauri/target/release/bundle/msi/`.
- Windows installer builds set `bundle.windows.webviewInstallMode=skip` and bundle `resources/MicrosoftEdgeWebview2Setup.exe` into both installer formats. NSIS also runs `src-tauri/windows/hooks.nsh` after installation; MSI relies on the app startup preflight to run the installed bootstrapper before the first WebView is created. This keeps both installer files compact, avoids Tauri bundler network timeouts, and repairs machines that do not already have WebView2 available; the target machine needs network access for the bootstrapper to download the Evergreen Runtime.
- MSI uses `src-tauri/windows/main.wxs`, a lightly customized Tauri WiX template. It keeps the `WixUI_InstallDir` directory chooser and explicitly declares elevated per-machine installation so Program Files installs request the required Windows permission instead of failing later with a directory-access error. NSIS is explicitly configured with `installMode=currentUser`, so the setup.exe path remains the no-admin default.
- Release builds initialize `tauri-plugin-log` for both debug and release, so startup/runtime failures can be diagnosed from the app log directory instead of disappearing behind the Windows GUI subsystem.
- On Windows, `main()` performs a WebView2 Runtime preflight before entering Tauri setup or constructing any WebView. It checks the per-user and per-machine WebView2 registry keys, attempts to run a bundled `MicrosoftEdgeWebview2Setup.exe` from the executable directory, `resources/`, or `TAURI_RESOURCE_DIR`, and exits with a clear message plus the official download page if the runtime is still unavailable.
- Portable builds are produced by `scripts/build-portable.mjs`.
- On Windows, the portable packager copies `typola.exe`, `MicrosoftEdgeWebview2Setup.exe`, a runtime note, and `Start-Typola.cmd` into a staging folder and emits `bundle/portable/*_windows-x64_portable.zip`. Users can launch `Typola.exe` directly: the Rust startup preflight checks the WebView2 Runtime before creating the app window, runs the bundled bootstrapper when the runtime is missing, and shows a visible error plus the official WebView2 download page if installation still fails. `Start-Typola.cmd` remains as a diagnostic fallback with the same preflight.
- The inner Windows `typola.exe` is an implementation detail of the installer / portable package, not a supported standalone release artifact. Official Windows distribution artifacts are the installer and the portable zip.
